/**
 * Nome/identidade do sistema — missão "Método Vetor" (2026-08-28). Constante central única: todo
 * texto de interface que precisa citar o nome do método ou do mentor (Professor) importa DAQUI,
 * nunca escreve o nome literal espalhado pelo código. Trocar o nome no futuro é editar só este
 * arquivo.
 *
 * "Vetor" — cada recomendação do sistema tem DIREÇÃO (o que estudar) e MAGNITUDE (o quanto isso
 * importa pra nota, via peso do código na prova × lacuna real do aluno — ver Motor 3,
 * src/lib/pedagogy/scoreEstimate.ts). O nome não é decorativo, descreve a mecânica real.
 */
export const NOME_METODO = "Método Vetor";

/** Como o Professor (mentor de IA) se apresenta e assina as mensagens — mesma entidade do
 * `/professor`, só com identidade de marca. Nunca renomear o `ProfessorFunction`/rotas técnicas. */
export const NOME_MENTOR = "Vetor";
